// import App from "../App.vue";
// import { createVuetifyWrapper } from "@/utils/VuetifyJestUtils.js";

describe("App 단위테스트", () => {
  // const wrapper = createVuetifyWrapper(App);
  it("$alert 실행하고 팝업이 뜨는지 확인한다.", () => {
    // const predictions = {
    //   title: "알림",
    //   msg: "알러트를 실행했어요.",
    // };
    // const results = getPopupDatas("alert");
    expect(1).toBe(1);
    // expect(results.title).toBe(predictions.title);
  });
});
